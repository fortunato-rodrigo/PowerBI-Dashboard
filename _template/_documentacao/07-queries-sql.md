# Queries SQL — [NomeDoDashboard]
> Gerado em: [data] | Script: `tools/Extract-QueryMetadata.ps1 -Dashboard [NomeDoDashboard]`
> Fonte: `data_quality.tables_in_dashboards_pbix`
> Para regenerar: `.\tools\Extract-QueryMetadata.ps1 -Dashboard [NomeDoDashboard] -Force`

---

## Resumo de linhagem

| Tabela Power BI | Camadas Databricks | Tabelas referenciadas | Alertas |
|---|---|---|---|
| `[tabela]` | gold, silver | N tabelas | — |

---

## Detalhamento por tabela

### `[nome_tabela]`

**Dashboard:** [nome completo]
**Ultima atualizacao na tabela Databricks:** [data]

**Tabelas Databricks referenciadas:**

- `gold.[tabela]` 🟢
- `silver.[tabela]` 🟡
- `bronze.[tabela]` ⚠️

**SQL completo:**

```sql
-- SQL gerado automaticamente por Extract-QueryMetadata.ps1
```

---
*Gerado por tools/Extract-QueryMetadata.ps1 | Remessa Online*
